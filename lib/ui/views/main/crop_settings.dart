import 'package:flutter/material.dart';

class CropSettingsV extends StatelessWidget {
  const CropSettingsV({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}